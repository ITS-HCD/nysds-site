---
permalink: /updates/
title: Whatʼs New
description: What is new in the New York State Design System?
section: Updates
navOrder: 1
heading_links: false
---

{% extends "layouts/base.njk" %}

{% block content %}
<div class="nys-grid-container-widescreen">

  <header class="page-header">
    <h1 class="page-title">Whatʼs New</h1>

    {%- if description -%}
      <p class="page-subtitle">Check out the latest updates to the New York State Design System.</p>
    {%- endif -%}

  </header>

  <section class="nys-grid-row nys-grid-gap">
    
    <ul class="post-list nys-grid-container">
      {%- for post in collections.updates -%}
      <li class="post-list__item{% if "article" in (post.data.tags | default([])) %} post-list--highlight{% endif %}">

            <h2 class="post-list__title">
              <a href="{{ post.url }}" class="post-list__link">
                {% if post.data.tags | hasReleaseTag %}<span class="release-mark" aria-hidden="true"></span>{% endif %}{{ post.data.title }}
              </a>
            </h2>

            <div class="post-list__description">
              {{ post.data.description }}
            </div>

            <div class="post-list__meta">  
              <div class="post-list__author">
                by {{ post.data.author }}
              </div>
              <time class="post-list__date" datetime="{{ post.data.date | dateISO }}">{{ post.data.date | dateFull }}</time>
            </div>

      </li>
      {%- endfor -%}
    </ul>

  </section>
</div>
{% endblock %}

{% block styles %}
{% endblock %}

{% block scripts %}
{% endblock %}
